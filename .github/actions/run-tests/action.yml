name: "Run Tests"
description: "Install Playwright browsers and run tests"
runs:
  using: "composite"
  steps:
    - name: Install Playwright browsers
      run: pnpm --filter @wordsearchy/frontend exec playwright install --with-deps chromium
      shell: bash

    - name: Run tests
      run: pnpm test:run
      shell: bash
